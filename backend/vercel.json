{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/share/gallery/:id",
      "destination": "/api/share/gallery/:id"
    },
    {
      "source": "/api/(.*)",
      "destination": "/server.js"
    }
  ]
}