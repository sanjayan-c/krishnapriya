export type Notification = {
    icon: string;
    variant: string;
    text: string;
    time: string;
}

export type ProfileOption = {
    icon: string;
    label: string;
    redirectTo: string;
}
